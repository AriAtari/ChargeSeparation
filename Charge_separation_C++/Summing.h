#include <iostream>
#include <cmath>
#include <string>
#include "Padplane.h"

void summing(padplane padplane,electron electronlist[]){
for(int e =0; e < (&electronlist)[1]-electronlist;e++){
    for(int po =0; po < (&electronlist[e].pointslist)[1]-electronlist[e].pointslist;po++){
        for(int pado =0; pado < (&padplane.padlist)[1]-padplane.padlist;pado++){
            

        }

    }

};
};